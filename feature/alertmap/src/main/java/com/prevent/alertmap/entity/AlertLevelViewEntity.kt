package com.prevent.alertmap.entity

import com.prevent.alertmap.entity.value.AlertLevelViewObject

internal data class AlertLevelViewEntity(
    val id: Int,
    val alertLevel: AlertLevelViewObject
)
